import React from "react";
import styled, { ThemeProvider } from "styled-components";
import Home from "../components/Hotel/home/Home";

function Main() {
	return (
		<React.Fragment>
			<Home />
		</React.Fragment>
	);
}

export default Main;
